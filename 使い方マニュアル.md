# コムデスク 自動投入マニュアル

Googleスプレッドシートの営業リストを、コムデスクへ自動で登録するツールです。
**リンクを1つ入れて実行すると、プロジェクト登録 → アサインユーザー設定 → 重複確認 → 送信 → 完了通知までを自動**で行います。

> このツールは一度セットアップすれば、**AI（このチャット）を使わず、ご自身のパソコンだけ**で動かせます。毎回のトークンは不要です。

---

## 目次

1. [はじめての準備（最初の1回だけ）](#1-はじめての準備最初の1回だけ)
2. [毎回の使い方](#2-毎回の使い方)
3. [うまくいかないとき](#3-うまくいかないとき)
4. [安全のしくみ（自動で止まる場合）](#4-安全のしくみ自動で止まる場合)

---

## 1. はじめての準備（最初の1回だけ）

> **Windowsの方へ**：コマンドを覚えなくても、`run.bat` をダブルクリックして
> 番号を選ぶだけで同じことができます（`1) 初回セットアップ` → `2) コムデスクにログイン`）。
> Macの方は `./run.sh` が同じメニューです。以下は中で何をしているかの説明です。

### 準備A：ソフトのインストール

1. パソコンに **Node.js（バージョン20以上）** を入れます（すでに入っていればスキップ）。
   - Windowsの場合：PowerShellで `winget install OpenJS.NodeJS.LTS` を実行し、
     終わったらPowerShellを開き直します。
   - Macの場合：<https://nodejs.org/ja> からLTS版を入れます。
2. このフォルダ（`hd-system-auto`）を開きます。
   - Macの場合：Finderでフォルダを右クリック →「フォルダに新規ターミナル」
   - Windowsの場合：エクスプローラでフォルダを開き、右クリック →「ターミナルで開く」
     （またはアドレスバーに `powershell` と入力してEnter）
3. 次を実行します（最初の1回だけ）。

   **かんたんな方法**

   ```powershell
   .\run.ps1 -Task setup
   ```

   ```bash
   ./run.sh --task setup
   ```

   **自分でコマンドを叩く場合**（Windows・Mac共通）

   ```bash
   npm install
   cd comdesk-playwright-importer
   npm install && npm run install-browser
   cd ..
   ```

### 準備B：コムデスクにログイン（最初の1回だけ）

ログイン情報はパスワードとして保存されず、ログイン状態だけが保存されます。切れたときだけやり直せばOKです。

1. 次を実行します。

   **かんたんな方法**：`run.bat`（Macは `./run.sh`）で `2) コムデスクにログイン` を選ぶ。

   **自分でコマンドを叩く場合**

   ```bash
   cd comdesk-playwright-importer
   npm run login
   ```

2. 自動で開いたChromeで、**いつも通りコムデスクにログイン**し、**「プロジェクト管理」画面**まで進みます。
3. ターミナル（PowerShell）に戻って **Enterキー** を押します。→ ログイン状態が保存されます。
4. 元のフォルダに戻ります。

   ```bash
   cd ..
   ```

これで準備は完了です。以降は「2. 毎回の使い方」だけで動きます。

---

## 2. 毎回の使い方

投入したいGoogleスプレッドシートは、**「リンクを知っている全員が閲覧可」** に共有しておいてください。

### ステップ1：まず確認だけ（登録はしない）

いきなり本番にせず、**読み込み件数やプロジェクト名を確認**します。コムデスクには何も登録されません。

**かんたんな方法**：`run.bat`（Macは `./run.sh`）で `5) スプレッドシートからコムデスクへ投入（確認）` を選び、リンクを貼り付けます。

**自分でコマンドを叩く場合**

```bash
npm run comdesk:auto:dry -- --spreadsheet-url="ここにGoogleシートのリンク"
```

- 表示された `projectName`（プロジェクト名）と件数が想定通りか確認します。
- プロジェクト名は住所から `都道府県_市区町村` の形で自動で決まります。

### ステップ2：本番投入

確認して問題なければ、`--execute` を付けて実行します。

**かんたんな方法**：`run.bat`（Macは `./run.sh`）で `6) スプレッドシートからコムデスクへ投入（本番）` を選びます。実行前に確認を求められます。

**自分でコマンドを叩く場合**

```bash
npm run comdesk:auto -- --spreadsheet-url="ここにGoogleシートのリンク" --execute
```

これで完了通知まで自動で進みます。ブラウザの画面がそのまま見えるので、動いている様子を確認できます。

### 複数のエリアを続けて投入したいとき

1つ終わったら、次のリンクで同じコマンドをもう一度実行するだけです。

```bash
# 例：伊勢原市 → 三浦市の順で
npm run comdesk:auto -- --spreadsheet-url="伊勢原市のリンク" --execute
npm run comdesk:auto -- --spreadsheet-url="三浦市のリンク" --execute
```

### 何件もまとめて投入したいとき（一括投入）

投入したいリンクが多いときは、リンクを**メモ帳に1行ずつ貼ったファイル**を用意すれば、上から順番にまとめて投入できます。

1. `config` フォルダの中の `comdesk-batch.example.txt` をコピーし、名前を `comdesk-batch.txt` に変えます。
2. そのファイルに、投入したいスプレッドシートのリンクを**1行ずつ**貼り付けて保存します（空行や、行の先頭が `#` の行は無視されます）。
3. `run.bat`（Macは `./run.sh`）を起動し、メニューから選びます。
   - `7) 複数シートを一括投入（確認）` … まず確認（登録なし）
   - `8) 複数シートを一括投入（本番）` … 本番投入

自分でコマンドを叩く場合は次のとおりです。

```bash
# 確認（何も登録されません）
npm run comdesk:batch:dry -- --list=config/comdesk-batch.txt

# 本番投入
COMDESK_EXECUTE=true npm run comdesk:batch -- --list=config/comdesk-batch.txt --execute
```

- 途中で1件うまくいかなくても、残りはそのまま続けます。最後に「成功○件・失敗○件」の一覧が出ます。
- リンクは「リンクを知っている全員が閲覧可」で共有しておいてください（1件ずつのときと同じ）。

---

## 3. うまくいかないとき

| 症状 | 対処 |
|---|---|
| 「ログイン」や「取得できません」と出て止まる | ログインが切れています。`準備B` の `npm run login` をもう一度実行してください。 |
| 「スプレッドシートを取得できません」 | シートの共有設定を「リンクを知っている全員が閲覧可」に変更してください。 |
| 「アクセスできるユーザーが…」で止まる | 画面上に指定ユーザーが見つからないと、誤登録防止のため停止します。コムデスクのユーザー一覧を確認してください。 |
| すでに「1人だけ」で登録されてしまった | 修復コマンドで付け直せます（下記）。 |
| （Windows）`node は認識されません` と出る | Node.jsが未インストールか、入れた後にPowerShellを開き直していません。 |
| （Windows）`スクリプトの実行がシステムで無効` と出る | `run.ps1` を直接ではなく `run.bat` から起動してください。 |
| （Windows）文字化けする | cmd.exeではなくPowerShell（またはWindows Terminal）で開いてください。 |

### すでに登録済みプロジェクトのアサインユーザーを直す

```bash
cd comdesk-playwright-importer
node src/repair-assign-users.js --project-name="神奈川県_伊勢原市" --workgroups="カフェ,居酒屋"
cd ..
```

`--workgroups` は直したいワークグループをカンマ区切りで指定します。

---

## 4. 安全のしくみ（自動で止まる場合）

このツールは、**あやしい状況では登録・送信をせずに安全に止まる**ように作られています。止まったときは画面のメッセージを確認してください。

- 想定外の確認ダイアログが出たとき
- アサインユーザーが全員そろわないとき（登録前・登録後の二重チェック）
- プロジェクト名・ワークグループが一意に特定できないとき
- 同じプロジェクトがすでにあるとき

止まっても、原因を直して同じコマンドをもう一度実行すれば大丈夫です。

---

### 参考：設定ファイルの場所

- 投入時の共通設定（アサインユーザー、重複チェックなど）：`comdesk-playwright-importer/config.json`
- 接続先URLや画面表示の設定：`comdesk-playwright-importer/.env`
- 本番投入のON/OFF：`.env` の `COMDESK_EXECUTE`（`true` で本番有効）
