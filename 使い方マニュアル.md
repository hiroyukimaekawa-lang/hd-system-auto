# コムデスク 自動投入マニュアル

Googleスプレッドシートの営業リストを、コムデスクへ自動で登録するツールです。
**リンクを1つ入れて実行すると、プロジェクト登録 → アサインユーザー設定 → 重複確認 → 送信 → 完了通知までを自動**で行います。

> このツールは一度セットアップすれば、**AI（このチャット）を使わず、ご自身のパソコンだけ**で動かせます。毎回のトークンは不要です。

---

## 目次

1. [はじめての準備（最初の1回だけ）](#1-はじめての準備最初の1回だけ)
2. [毎回の使い方](#2-毎回の使い方)
3. [うまくいかないとき](#3-うまくいかないとき)
4. [安全のしくみ（自動で止まる場合）](#4-安全のしくみ自動で止まる場合)

---

## 1. はじめての準備（最初の1回だけ）

### 準備A：ソフトのインストール

1. パソコンに **Node.js（バージョン20以上）** を入れます（すでに入っていればスキップ）。
2. **ターミナル**を開き、このフォルダ（`hd-system-auto`）へ移動します。
   - Macの場合：Finderでフォルダを右クリック →「フォルダに新規ターミナル」
3. 次の3行を1行ずつ実行します（最初の1回だけ）。

   ```bash
   npm install
   cd comdesk-playwright-importer
   npm install && npm run install-browser
   cd ..
   ```

### 準備B：コムデスクにログイン（最初の1回だけ）

ログイン情報はパスワードとして保存されず、ログイン状態だけが保存されます。切れたときだけやり直せばOKです。

1. ターミナルで次を実行します。

   ```bash
   cd comdesk-playwright-importer
   npm run login
   ```

2. 自動で開いたChromeで、**いつも通りコムデスクにログイン**し、**「プロジェクト管理」画面**まで進みます。
3. ターミナルに戻って **Enterキー** を押します。→ ログイン状態が保存されます。
4. 元のフォルダに戻ります。

   ```bash
   cd ..
   ```

これで準備は完了です。以降は「2. 毎回の使い方」だけで動きます。

---

## 2. 毎回の使い方

投入したいGoogleスプレッドシートは、**「リンクを知っている全員が閲覧可」** に共有しておいてください。

### ステップ1：まず確認だけ（登録はしない）

いきなり本番にせず、**読み込み件数やプロジェクト名を確認**します。コムデスクには何も登録されません。

```bash
npm run comdesk:auto:dry -- --spreadsheet-url="ここにGoogleシートのリンク"
```

- 表示された `projectName`（プロジェクト名）と件数が想定通りか確認します。
- プロジェクト名は住所から `都道府県_市区町村` の形で自動で決まります。

### ステップ2：本番投入

確認して問題なければ、`--execute` を付けて実行します。

```bash
npm run comdesk:auto -- --spreadsheet-url="ここにGoogleシートのリンク" --execute
```

これで完了通知まで自動で進みます。ブラウザの画面がそのまま見えるので、動いている様子を確認できます。

### 複数のエリアを続けて投入したいとき

1つ終わったら、次のリンクで同じコマンドをもう一度実行するだけです。

```bash
# 例：伊勢原市 → 三浦市の順で
npm run comdesk:auto -- --spreadsheet-url="伊勢原市のリンク" --execute
npm run comdesk:auto -- --spreadsheet-url="三浦市のリンク" --execute
```

---

## 3. うまくいかないとき

| 症状 | 対処 |
|---|---|
| 「ログイン」や「取得できません」と出て止まる | ログインが切れています。`準備B` の `npm run login` をもう一度実行してください。 |
| 「スプレッドシートを取得できません」 | シートの共有設定を「リンクを知っている全員が閲覧可」に変更してください。 |
| 「アクセスできるユーザーが…」で止まる | 画面上に指定ユーザーが見つからないと、誤登録防止のため停止します。コムデスクのユーザー一覧を確認してください。 |
| すでに「1人だけ」で登録されてしまった | 修復コマンドで付け直せます（下記）。 |

### すでに登録済みプロジェクトのアサインユーザーを直す

```bash
cd comdesk-playwright-importer
node src/repair-assign-users.js --project-name="神奈川県_伊勢原市" --workgroups="カフェ,居酒屋"
cd ..
```

`--workgroups` は直したいワークグループをカンマ区切りで指定します。

---

## 4. 安全のしくみ（自動で止まる場合）

このツールは、**あやしい状況では登録・送信をせずに安全に止まる**ように作られています。止まったときは画面のメッセージを確認してください。

- 想定外の確認ダイアログが出たとき
- アサインユーザーが全員そろわないとき（登録前・登録後の二重チェック）
- プロジェクト名・ワークグループが一意に特定できないとき
- 同じプロジェクトがすでにあるとき

止まっても、原因を直して同じコマンドをもう一度実行すれば大丈夫です。

---

### 参考：設定ファイルの場所

- 投入時の共通設定（アサインユーザー、重複チェックなど）：`comdesk-playwright-importer/config.json`
- 接続先URLや画面表示の設定：`comdesk-playwright-importer/.env`
- 本番投入のON/OFF：`.env` の `COMDESK_EXECUTE`（`true` で本番有効）
