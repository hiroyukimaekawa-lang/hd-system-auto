# HD AIアシスタント Obsidian IA実装パッケージ

## 配置

```bash
cd "/Users/maekawahiroyuki/hd-system-auto"
unzip -o "$HOME/Downloads/HD_AIアシスタント_Obsidian_IS_FS_CS_ClaudeCodeパッケージ.zip"
```

## Claude Code起動

```bash
cd "/Users/maekawahiroyuki/hd-system-auto"
claude
```

## Claude Codeへ入力

```text
docs/implementation-prompts/CLAUDE_CODE_HD_OBSIDIAN_IS_FS_CS_ARCHITECTURE.md を最初から最後まで読み、記載内容をすべて実装してください。
```

既存Vaultファイルを移動・改名・削除せず、現在の `HD事業部/商談支援` を維持したまま、MOC/Wikiリンクで `HDシステム → IS / FS / CS → 各機能` のGraph構造を追加します。
